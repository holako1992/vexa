- **Upcoming page and calendar health (#1678).** A new `/upcoming` page groups planned meetings
  by day, with the source calendar's chip, a per-meeting Join / Don't join override, and the
  producer's own auto-join skip reason shown verbatim. A new `/calendar` page shows each
  connection's last sync time, last error, events touched, and a Reconnect action for a
  connection whose grant needs it. See [Calendar sync](/how-to/calendar-sync).
