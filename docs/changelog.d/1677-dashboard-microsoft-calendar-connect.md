- **Connect Microsoft 365, beside Connect Google Calendar (#1677).** The dashboard's Add Bot
  dialog now offers one-click Microsoft 365 calendar connect, sharing its OAuth callback page
  with the existing Google flow (`/calendar/microsoft/callback`). A connection whose Microsoft
  grant needs re-authorizing shows the same Reconnect action Google connections already had. See
  [Calendar sync](/how-to/calendar-sync).
