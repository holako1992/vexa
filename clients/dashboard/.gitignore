# Build + tooling output. Mirrors clients/terminal/.gitignore — the image builds these itself
# (see Dockerfile), so nothing here belongs in the tree.
/.next
/out
/node_modules
next-env.d.ts
*.tsbuildinfo
.env
.env.*
!.env.example

# Playwright run output (e2e). Regenerated every run.
/test-results
/playwright-report
